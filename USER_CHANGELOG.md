# 产品更新

## 最新

- 修复首尾帧参考选帧的坑：先选首帧时尾帧下拉不再消失（现在选任一帧都会固定当前模式），选完也会立刻写进节点，重新打开画布仍能看到你选的首帧和尾帧。
- 画布助手或命令行准备的首尾帧视频节点，打开网页时模式下拉和「参考帧」会显示实际选择，不再是一片空白。
- 视频节点的时长和分辨率现在能读到真实值（生成落盘和读取时各解析一次视频头），确认「是不是 15 秒、是不是 16:9」不用再靠猜；拿不到时如实说明，不会显示成 0。
- 画布助手只改生成规格（画幅、时长、分辨率、音频）时不必再顺手改一次节点标题；写进去的规格在读取和审批摘要里都能看到。
- 提示词已用 MiniMax-H3 原生 `<Picture 1>` 这类写法写全素材时，不再重复追加一段「资产参考」；已有的「资产参考」段落也不会变成两段。
- 提交生成前会先给出预估积分（`linggan` 命令行的确认摘要和画布助手的确认提示），让你在花钱之前看到价格。
- 画布助手可以撤销自己刚建、还没写内容也没提交生成的空节点；你和它已经用过的节点仍然删不掉。
- 素材节点现在能读到真实时长和分辨率，视频生成完可以确认结果是否可用；读取接口不会下载媒体，也不对外暴露存储地址。
- 节点很多时，命令行读取画布不再漏掉跨页的连线，并给出连线总数；同一份提示词重复提交不会出现两段「资产参考」。
- 新增 `linggan` 命令行，供外部 Agent 登录后创建或打开自己的画布、读取摘要、按受控操作改节点和上传素材。提交生成前会在对话里给出摘要，你同意后它才提交，不会调用系统里的语言模型。
- 网页和命令行同时改一张画布时，只要改的不是同一个位置就会自动合并后继续保存，不再直接停在“版本冲突”；改到同一处时仍会保留本地草稿并提示。
- 全能参考模式下，素材入口按同名模型组的整体能力开放。此前还没加素材时页面会选中能力最弱的线路，导致第一张参考图加不进去。
- 画布视频节点正在播放时，点击画布空白或其它节点会暂停，当前播放进度保留；点击节点自身、工具条或提示词面板不会打断播放。
- 模型选择器里会显示渠道标签（如“限时特价”“官方1折”）；同名模型来自不同渠道时标签不会串用，价格、报价和请求路由不受影响。
- 画布助手的名称、面板标题、欢迎语和输入提示会跟随站点设置显示，可能不是默认名称；启用自定义形象时右下角助手会渲染为模型动画，加载失败仍保留可点击的默认入口。
- 修复画布引用刚生成或刚重命名的素材时误报“没有对应的画布资源”；同一素材已绑定画布节点时，@ 候选只显示一项，并同时展示素材名称和稳定的画布引用编号。

- 视频创作新增“文生视频、图生视频、首尾帧参考、全能参考”四种同级模式，创作页和画布视频节点的模式下拉统一放在模型旁；切换模式会保留已添加素材和画布连线，只在本次生成时使用当前模式需要的参考内容。
- 项目画风入口移到画布“添加节点”的首要位置；分镜缺少画风时可在节点内直接设置，项目侧栏的未设置状态也可点击，不再只有提示却找不到入口。
- 修复生产构建中 CSS 加载顺序导致的画布和 Agent 间距、布局异常，节点悬浮控件和工具类样式保持稳定。
- 画布 Agent 改为更清晰的磨砂面板，新增品牌欢迎入口，执行权限改用状态图标，模型选择和发送操作更紧凑。
- 工作台账户菜单和筛选下拉统一为柔和表面；素材库三档显示密度现在会真实调整卡片大小，并保留键盘焦点提示。
- 积分充值新增易支付渠道，管理员配置后可按网关能力使用跳转或二维码支付。
- 修复部分网关环境下编辑并保存系统渠道模型时报 HTTP 400 的问题。
- 画布可隐藏普通节点连线，悬浮、聚焦或选中节点时只显示相关连线，复杂画布更清爽。
- 文本节点字号会随节点尺寸联动，并保持可读的大小范围。
- 画布底部素材入口显示素材数量，选择与抓手切换的图标和键盘操作更清晰。
- 远程图片、视频和音频统一使用稳定资源地址；生成视频完成缓存校验后才显示成功，减少成功后无法播放的情况。
- 画布节点、加号和悬浮控件恢复正确的显隐与布局。
- 画布助手模型列表限制在视口内滚动，不再撑出窗口。
- 图片、视频和音频的访问出口统一由服务端按存储分发设置决定：配了 CDN 就直连 CDN，没配就签名回源，需要字节的场景走平台读取。**对象存储与 CDN 需要对站点来源开放 CORS**（`Access-Control-Allow-Origin` 即可），否则复制图片、导出和本地缓存这类读字节的操作会失败并如实报错。
- 修复对象存储/CDN 下点击下载图片会在浏览器里打开、而不是保存文件的问题。
- 修复上传到画布的文本保存后再打开会变成 `/api/resources/.../file` 地址的问题。
- 打开画布时图片预览改为直连对象存储/CDN，不再把历史 S3 原图全部经服务器下载，界面不会等到全部加载完才能点。
- 画布助手更跟当前要求走：对话中途改口会带到下一轮，取消的计划不会被催着做完；只改字或做分析时不会擅自生图、改剧情。
- 助手生成失败和写回画布失败会分开说明；刷新后旧助手事件不再把已同步内容反复打成冲突。
- 画布保存会核对云端版本，冲突时保留本地草稿；可从右侧版本记录预览和恢复历史。
- 批量创作表支持全局提示词、参考图上传替换、列排序和拖拽交换。
- 图片/视频提示词放大窗口可拖拽改尺寸，方向键微调。
- 画布已有视频可从工具条「高清」一键提高清晰度：可选清晰度、帧率和版本，结果另存为新视频，不覆盖原片；该能力不出现在视频模型列表中。
- 工作台首页可展示管理员配置的常驻滚动通知；侧栏容量加了硬盘图标，积分入口改为礼盒标。
- TT Image 2.5 生成设置补齐标准/增强版、画质和透明底，比例增加自适应、2:1、1:2、9:21。
- Midjourney 生成设置补齐比例、MJ/Niji 模式、精细度、风格化和变化参数；即梦 5.0 分辨率可在 2K 和 3K 间切换。
- 修复问鼎图片/视频部分模型不发送比例和分辨率的问题，香蕉、即梦、千问、TT Image、MiniMax H3 Max、万相 3.0 现在会按上游参数出图出片。
- 生成图片/视频时的状态统一显示「生成中」并带进度条，不再出现「排队」「上游生成中」这类阶段措辞。
- 模型选择器改为单层列表：打开就能直接选模型，不再先选渠道分组；同名模型相邻排列并可按渠道区分。
- 统一暗黑模式下的消息提示、通知、悬浮说明和弹层配色，更新日志弹窗排版与滚动更整齐。
- 模型选择器里模型名和价格不再互相挤压；插件页切换分类后回到列表顶部。
- 创作首页的 Agent 模式按钮增加呼吸高光，更容易发现。
- 画布新增「发送到 Agent」：多选节点后点选区工具栏的 @ 图标，或在节点右键菜单里选择，即可把这些节点引用直接带进画布助手输入框。
- 用户工作台整体改版：侧栏加宽并按模块分色，资产、技能、插件页统一为内容库布局，顶栏新增积分入口和账户卡片。
- 创作首页输入区收窄居中，模式切换更紧凑，新增灵感入口。
- 画布菜单新增「保存」与「强制覆盖保存」：云端同步反复失败时可以按确认弹窗用本地内容覆盖云端。
- 助手生成图片的审批卡可以直接切换模型、画幅和质量，只影响本次生成；审批期间移动参考图不再中断生成。
- 助手对话不再限制 8 轮，历史记录按长度而不是轮数控制。
- 新增 Agnes 图像模型支持，可选择画面比例与单次生成张数。
- 视频提示词长度上限从 1000 提高到 8000 字符，长分镜与技能上下文不再被本地拦截。
- 修复画布原地重生成后素材绑定错乱、素材库远端空结果遮蔽本地素材、素材被已结束任务阻塞无法删除等问题。
- 修复系统渠道模型改名后价格档仍把旧上游 ID 发给供应商的问题。
- 修复画布生成图片重命名后仍显示为「图片」的问题；原位再次生成和结果回写都会保留自定义名称。
- 修复短剧章节和画布分镜任务完成后无法恢复镜头行的问题，分镜结果现在会按结构化模板生成并直接写入分镜表。
- 修复问鼎 LK888 MiniMax H3 使用 768P 规格时价格档无法命中的问题。
- 画布助手可做结构化分镜：多镜头任务可建分镜表，按镜头逐条增删改；简单单镜头仍可用普通节点。
- 助手按参考图筛选视频模型，取消后可在原节点继续生成并重新审批，不重复建节点。
- 助手工具轨迹按读取 / 创建 / 操作分组，重复的媒体生成事件会合并；审批卡更清楚说明改了什么。
- 未支持的节点不再挡住整张画布的助手阅读。
- 画布助手改为云端 Agent 浮窗：可拖动、调整大小，刷新后保持位置；对话发出后跟随到底部，向上翻看时暂停跟随。
- 助手可阅读当前画布摘要并保留会话；本机 Runtime 入口已去掉，创作在网站内完成。
- 新增全景图：可从图片创建全景节点，支持全屏环视、方向键旋转、截图当前视角和四向视图导出。
- 打光与镜头确认后先落下可编辑的空图片节点，可继续改提示词、模型和尺寸再生成。
- 大画布图片打开更快，优先显示当前视野内的图，刷新后可直接走本地缓存。
- 拖拽连线可替换提示词里的参考图，引用编号会一并同步。
- 视频生成使用 @文本 引用时，会把文本节点的真实内容带给模型。
- 创作页可用视频、图片、文本胶囊切换生成类型；素材库支持方舟素材引用和人像认证标记。
- 复制画布图片更稳定。
- 新增多角度贴节点与宫格切分菜单：多选节点时虚线框会跟随移动。
- 修复重新生成已有图片时，旧结果被错误当作参考图自动连线的问题。
- 视频生成在弱网或接口暂态失败时更稳定，不再轻易中断任务。
- 画布助手修复审批预览反复弹出的问题，读取大量节点时自动折叠明细，回复文字即时显示。

## v1.2.8.rc1

- 新增灰度、边缘、线稿、深度和姿态等本地媒体转换能力，转换结果可直接用于后续生成。
- 优化画布选择、整理和工具栏交互，提升复杂画布的操作效率。
- 完善媒体上传预览、节点连线、任务恢复和素材同步体验。
