# 问鼎 LK888 Audio

该目录是独立官方协议插件源码。后端从生成的 `lk888-audio.yingce-plugin` 包加载，不依赖系统内置 `host:` 适配器。

覆盖豆包语音合成 2.0（`doubao-tts-2.0`）：文字转语音，支持上百种预置音色、多情感、多语种与长文本（最长 10 万字符）。完整接口见 [docs/interface.md](docs/interface.md)。
